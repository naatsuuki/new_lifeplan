<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AgeMap</title>
  <style>
    body {
      font-family: 'Helvetica', sans-serif;
      background-color: #f0f4f7; /* メインの背景色 */
      color: #333; /* メインのテキストカラー */
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: flex-end;
    }
    .container {
      display: flex;
      max-width: 800px;
      margin: 20px;
      padding: 20px;
      background-color: #fff; /* コンテンツの背景色 */
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .sidebar {
      width: 200px; /* サイドバーの幅 */
      padding: 20px;
      background-color: #e0e0e0; /* サイドバーの背景色 */
      border-left: 1px solid #ccc;
    }
    .sidebar ul {
      list-style-type: none;
      padding: 0;
    }
    .sidebar li {
      margin-bottom: 10px;
    }
    .sidebar a {
      color: #333;
      text-decoration: none;
    }
    .sidebar a:hover {
      text-decoration: underline;
    }
    .content {
      flex: 1; /* コンテンツ領域を伸縮可能に */
      padding: 20px;
    }
    h1 {
      color: #007bff; /* メインの見出しの色 */
    }
    p {
      font-size: 1.2em;
      line-height: 1.6;
    }
    form {
      margin-top: 20px;
    }
    label {
      display: block;
      margin-bottom: 8px;
    }
    input[type="text"], input[type="number"] {
      width: 100%;
      padding: 8px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }
    button {
      background-color: #007bff;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <h1>AgeMap</h1>
      <p>LifePlanを考えるお手伝いをします</p>
      <p>あなたと身近な人の年齢を比べてみましょう</p>

      <%= form_with(model: @user, url: users_path, method: :post) do |form| %>
        <%= form.label :name, '名前' %>
        <%= form.text_field :name %>

        <%= form.label :birth_year, '生まれ年（西暦）' %>
        <%= form.number_field :birth_year, min: 1900, max: Date.today.year %>

        <%= form.submit '保存する' %>
      <% end %>

      <ul class="user-list">
        <% @users.each do |user| %>
          <li>
            <span><%= user.name %> (<%= user.birth_year %>)</span>
            <%= button_to '削除', user_path(user), method: :delete, data: { confirm: '本当に削除しますか？' } %>
          </li>
        <% end %>
      </ul>
    </div>
    <div class="sidebar">
      <h2>メニュー</h2>
      <ul>
        <li><%= link_to 'ユーザー一覧', users_path %></li>
      </ul>
    </div>
  </div>
</body>
</html>
